{
    "luau-lsp.sourcemap.rojoProjectFile": "default.project.json",

    // Project settings for RequireOnRails live in requireonrails.json, which the CLI and the
    // CI action read too. Only editor preferences belong here.
    "require-on-rails.startsImmediately": false
}
